# 🚀 题库网页部署指南

`index.html` 是独立文件（CSS/JS 全部内联），拖到任何静态托管平台即可访问。

---

## ⚡ 方案一：GitHub Pages（最简，5分钟）

**无需域名、无需服务器、全免费。**

### 步骤

1. **创建 GitHub 仓库**
   - 打开 https://github.com/new
   - Repository name: `ti-ku`（随意取名）
   - 勾选 Public，点 Create

2. **上传文件**
   - 点 `uploading an existing file`
   - 把 `index.html` 拖进去
   - Commit message 写 `题库部署`，点 Commit

3. **开启 Pages**
   - 进仓库 → Settings → Pages
   - Branch 选 `main`，点 Save
   - 等 1 分钟后访问：`https://你的用户名.github.io/ti-ku/`

4. **完成！** 链接可以发给任何人，手机电脑都能打开。

---

## 🌐 方案二：DigitalPlat 免费域名 + Cloudflare Pages

**适合想要自己域名的场景，完全免费。**

### 一、申请免费域名（5分钟）

1. 打开 https://dash.domain.digitalplat.org/
2. 用 GitHub 登录授权
3. 给 https://github.com/DigitalPlatDev/FreeDomain 点 ⭐
4. 申请域名，后缀选 `.dpdns.org` 或 `.qzz.io`
5. 先留空 DNS，提交

### 二、绑定 Cloudflare（10分钟）

1. 注册/登录 https://dash.cloudflare.com/
2. 添加站点 → 输入你的域名（如 `ti-ku.dpdns.org`）
3. Cloudflare 会给你两个 NS 地址（如 `alice.ns.cloudflare.com`）
4. 回到 DigitalPlat 控制台，把这两个 NS 填进域名 DNS 设置
5. 等 5-30 分钟，Cloudflare 显示"活动"即成功

### 三、部署到 Cloudflare Pages

1. Cloudflare 左侧菜单 → Workers & Pages → Create → Pages
2. 选择 `Upload assets`
3. 项目名随意，把 `index.html` 拖进去
4. 点 Deploy → 得到 `xxx.pages.dev` 地址
5. 进入项目 → Custom domains → 添加你的 DigitalPlat 域名
6. 自动配置 DNS + SSL

**完成！** 现在 `https://ti-ku.dpdns.org` 就是你的题库网站。

---

## 🪞 方案三：Vercel（备选）

1. 打开 https://vercel.com/new
2. 导入 GitHub 仓库（需先完成方案一的步骤1-2）
3. 自动部署，获得 `xxx.vercel.app` 地址

---

## 📱 方案四：本机直接访问

如果只是自己用，不需要部署：

```bash
# Windows (PowerShell)
cd D:\code\resourse\bilibili\Playwright CLI + Skill\智慧树题库\deploy
python -m http.server 8080

# 浏览器打开 http://localhost:8080
```

---

## 📋 三种方案对比

| 方案 | 域名 | 免费 | 难度 | 国内速度 |
|------|------|------|------|---------|
| GitHub Pages | `xxx.github.io` | ✅ | ⭐ | ⭐⭐ |
| Cloudflare Pages | 自定义 `.dpdns.org` | ✅ | ⭐⭐⭐ | ⭐⭐⭐ |
| Vercel | `xxx.vercel.app` | ✅ | ⭐⭐ | ⭐⭐ |
| 本地 HTTP Server | `localhost:8080` | ✅ | ⭐ | ⭐⭐⭐⭐⭐ |
